echo 'we are committing ~/.bash_history so we can bind mount it' 
echo 'however we\'re also adding it to .gitignore to avoid bloat -- but feel free to comment it in .gitignore if you\'d like to preserve history in your repo' 
echo "however we're also adding it to .gitignore to avoid bloat -- but feel free to comment it in .gitignore if you'd like to preserve history in your repo"
clear
history
